// ===============================//  PolicyGrid Simulator (Revised)//  Classic Animated Version// ===============================// DOM Elementsconst nodeGrid = document.getElementById("nodeGrid");const firewallBar = document.getElementById("firewallBar");const rehabBar = document.getElementById("rehabBar");const logBox = document.getElementById("log");const runBtn = document.getElementById("runBtn");// Create 10 default nodeslet nodes = [];const TOTAL_NODES = 10;// Append message to logfunction log(message) {    logBox.innerHTML += "• " + message + "<br>";    logBox.scrollTop = logBox.scrollHeight;}// Reset & create node gridfunction createNodeGrid() {    nodeGrid.innerHTML = "";    nodes = [];    for (let i = 1; i <= TOTAL_NODES; i++) {        let div = document.createElement("div");        div.className = "node";        div.style.background = "#185c26";      // Alok Intelligence green        div.textContent = `N${i}\nAlok Intelligence`;        nodeGrid.appendChild(div);        nodes.push({            id: i,            element: div,            state: "normal"        });    }}// Animate firewall scanning every nodefunction runFirewallScan() {    log("Activating Eye-of-Shiv Firewall...");    firewallBar.style.width = "0%";    let index = 0;    let interval = setInterval(() => {        let percent = Math.min((index + 1) * (100 / TOTAL_NODES), 100);        firewallBar.style.width = percent + "%";        let node = nodes[index];        if (node) {            node.state = "flagged";            node.element.style.background = "#8b1e1e"; // red            node.element.textContent = `N${node.id}\nFLAGGED`;            log(`⚠️ Node N${node.id} flagged for misuse.`);        }        index++;        if (index >= TOTAL_NODES) {            clearInterval(interval);            setTimeout(startRehabCycle, 600);        }    }, 600);}// Animate healing cycle node-by-nodefunction startRehabCycle() {    log("Starting rehabilitation cycle...");    rehabBar.style.width = "0%";    let flaggedNodes = nodes.filter(n => n.state === "flagged");    let index = 0;    let interval = setInterval(() => {        let percent = Math.min((index + 1) * (100 / flaggedNodes.length), 100);        rehabBar.style.width = percent + "%";        let node = flaggedNodes[index];        if (node) {            node.state = "restored";            node.element.style.background = "#c28b1d"; // yellow healing            node.element.textContent = `N${node.id}\nHEALING`;            log(`🩺 Node N${node.id} undergoing healing cycle...`);        }        setTimeout(() => {            if (node) {                node.element.style.background = "#185c26"; // restored green                node.element.textContent = `N${node.id}\nRESTORED`;                log(`✅ Node N${node.id} restored successfully.`);            }        }, 400);        index++;        if (index >= flaggedNodes.length) {            clearInterval(interval);            log("🏁 Simulation complete.");        }    }, 700);}// Start entire simulationfunction startSimulation() {    // Reset UI    logBox.innerHTML = "";    firewallBar.style.width = "0%";    rehabBar.style.width = "0%";    createNodeGrid();    log("Initializing PolicyGrid Simulation...");    setTimeout(runFirewallScan, 800);}// Button eventrunBtn.addEventListener("click", startSimulation);// Render initial gridcreateNodeGrid();