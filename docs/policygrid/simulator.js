const nodeGrid = document.getElementById("nodeGrid");const firewallBar = document.getElementById("firewallBar");const rehabBar = document.getElementById("rehabBar");const logBox = document.getElementById("log");const runBtn = document.getElementById("runBtn");const NODE_COUNT = 10;let nodes = [];function log(msg) {    logBox.innerHTML += msg + "<br>";    logBox.scrollTop = logBox.scrollHeight;}function createNodes() {    nodeGrid.innerHTML = "";    nodes = [];    for (let i = 1; i <= NODE_COUNT; i++) {        const div = document.createElement("div");        div.className = "node";        div.textContent = `N${i}\nAlok Intelligence`;        nodeGrid.appendChild(div);        nodes.push({            id: i,            state: "normal",            element: div        });    }}function simulateFirewall() {    log("🔱 Eye of Shiv Firewall scanning...");    let progress = 0;    firewallBar.style.width = "0%";    const interval = setInterval(() => {        progress += 20;        firewallBar.style.width = progress + "%";        if (progress / 20 - 1 < nodes.length) {            const node = nodes[progress / 20 - 1];            node.state = "flagged";            node.element.style.background = "#5a1d1d";            node.element.textContent = `N${node.id}\nFLAGGED`;            log(`⚠️ Node N${node.id} flagged for misuse.`);        }        if (progress >= 100) {            clearInterval(interval);            simulateRehab();        }    }, 600);}function simulateRehab() {    log("🩺 Rehabilitation cycle started...");    let progress = 0;    rehabBar.style.width = "0%";    const flagged = nodes.filter(n => n.state === "flagged");    let index = 0;    const interval = setInterval(() => {        progress += 20;        rehabBar.style.width = progress + "%";        if (flagged[index]) {            const node = flagged[index];            node.state = "rehabilitated";            node.element.style.background = "#1d5a3a";            node.element.textContent = `N${node.id}\nRESTORED`;            log(`✅ Node N${node.id} rehabilitated.`);            index++;        }        if (progress >= 100) {            clearInterval(interval);            log("🏁 Simulation complete.");        }    }, 600);}function start() {    logBox.innerHTML = "";    firewallBar.style.width = "0%";    rehabBar.style.width = "0%";    createNodes();    log("Initializing PolicyGrid System...");    setTimeout(simulateFirewall, 500);}runBtn.addEventListener("click", start);createNodes();