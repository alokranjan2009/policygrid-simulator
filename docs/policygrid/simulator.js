const nodeGrid = document.getElementById("nodeGrid");const logBox = document.getElementById("logBox");const firewallBar = document.getElementById("firewallBar");const rehabBar = document.getElementById("rehabBar");const runBtn = document.getElementById("runBtn");function log(msg) {logBox.innerHTML += "• " + msg + "<br>";logBox.scrollTop = logBox.scrollHeight;}function resetUI() {nodeGrid.innerHTML = "";firewallBar.style.width = "0%";rehabBar.style.width = "0%";logBox.innerHTML = "";}function deployNodes() {for (let i = 1; i <= 10; i++) {const node = document.createElement("div");node.className = "node";node.innerHTML = `N${i}<br>Alok Intelligence`;nodeGrid.appendChild(node);}}runBtn.addEventListener("click", () => {resetUI();log("Initializing PolicyGrid...");setTimeout(() => {log("Activating Eye of Shiv Firewall...");firewallBar.style.width = "100%";```setTimeout(() => {  log("Transferring authority to Alok Intelligence...");  deployNodes();  setTimeout(() => {    log("Rehabilitating compromised actors...");    rehabBar.style.width = "100%";    setTimeout(() => {      log("Simulation complete. Network stabilized.");    }, 1000);  }, 1000);}, 1000);```}, 500);});