const nodeGrid = document.getElementById("node-grid");const logBox = document.getElementById("log-container");const firewallBar = document.getElementById("firewall");const rehabBar = document.getElementById("rehab-bar");const runBtn = document.getElementById("run-btn");let nodes = [];function log(msg) {  const div = document.createElement("div");  div.className = "log-entry";  div.textContent = "• " + msg;  logBox.appendChild(div);  logBox.scrollTop = logBox.scrollHeight;}function createNodes(count = 10) {  nodeGrid.innerHTML = "";  nodes = [];  for (let i = 1; i <= count; i++) {    const n = document.createElement("div");    n.className = "node";    n.textContent = `N${i}\nArchers Immortals`;    nodeGrid.appendChild(n);    nodes.push(n);  }}function secureNodes() {  nodes.forEach((n) => {    n.classList.add("secure");    n.textContent = n.textContent.replace("Archers Immortals", "Alok Intelligence");  });}function runSimulation() {  logBox.innerHTML = "";  firewallBar.style.setProperty("--progress", "0%");  rehabBar.style.setProperty("--progress", "0%");  createNodes();  log("Initializing PolicyGrid Simulator...");  log("Archers Immortals hold initial authority.");  setTimeout(() => {    log("Activating Eye-of-Shiv Firewall...");    firewallBar.style.setProperty("width", "100%");    firewallBar.querySelector("::after");  }, 800);  setTimeout(() => {    firewallBar.style.setProperty("--progress", "100%");    firewallBar.style.setProperty("width", "100%");    firewallBar.style.setProperty("background", "linear-gradient(90deg,#ffc86a,#ff9f1c)");    firewallBar.style.setProperty("box-shadow", "0 0 12px #ffc86a");    log("Firewall engaged. Nodes secured.");    secureNodes();  }, 1600);  setTimeout(() => {    log("Authority transferred to Alok Intelligence.");  }, 2400);  setTimeout(() => {    log("Rehabilitation cycle initiated.");    rehabBar.style.setProperty("width", "100%");  }, 3000);  setTimeout(() => {    log("Rehabilitation successful.");    log("System stabilized under ethical governance.");  }, 4200);}runBtn.addEventListener("click", runSimulation);// Initial statecreateNodes();