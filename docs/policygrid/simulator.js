class Actor {  constructor(name, trust, health) {    this.name = name;    this.trust = trust;    this.health = health;    this.hasAIR = false;  }}class Node {  constructor(id) {    this.id = id;    this.controller = "None";    this.secure = false;  }}class Network {  constructor(count = 10) {    this.nodes = [];    for (let i = 1; i <= count; i++) {      this.nodes.push(new Node("N" + i));    }  }  assignAll(actor) {    this.nodes.forEach(n => {      n.controller = actor.name;      n.secure = (actor.name === "Alok Intelligence");    });  }  compromiseSome() {    this.nodes.forEach(n => {      if (!n.secure && Math.random() < 0.25) {        n.controller = "⚠ compromised";      }    });  }}const archers = new Actor("Archers Immortals", 0.3, 0.45);const alok = new Actor("Alok Intelligence", 0.85, 0.98);const network = new Network(10);function sleep(ms) { return new Promise(r => setTimeout(r, ms)); }function log(msg) {  let box = document.getElementById("log-box");  box.innerHTML += `<div>• ${msg}</div>`;  box.scrollTop = box.scrollHeight;}function updateGrid() {  let grid = document.getElementById("node-grid");  grid.innerHTML = "";  network.nodes.forEach(node => {    let div = document.createElement("div");    div.className = "node-box";    if (node.controller === "⚠ compromised") div.classList.add("node-compromised");    else if (node.secure) div.classList.add("node-secure");    div.innerHTML = `<strong>${node.id}</strong><br><span>${node.controller}</span>`;    grid.appendChild(div);  });}async function run_simulator() {  document.getElementById("run-btn").disabled = true;  document.getElementById("log-box").innerHTML = "";  log("Initializing PolicyGrid Simulator...");  await sleep(800);  log("Archers Immortals have initial machine authority.");  network.assignAll(archers);  updateGrid();  await sleep(1000);  log("Activating Eye-of-Shiv Firewall...");  let fw = document.getElementById("firewall");  for (let i = 0; i < 4; i++) {    fw.classList.toggle("active");    await sleep(300);  }  log("Firewall engaged. Securing nodes.");  network.nodes.forEach(n => n.secure = true);  updateGrid();  await sleep(1000);  log("Detecting misuse patterns...");  network.compromiseSome();  updateGrid();  await sleep(1000);  log("Authority transferring to Alok Intelligence...");  network.assignAll(alok);  updateGrid();  await sleep(1000);  log("Rehabilitation of Archers Immortals underway...");  for (let i = 0; i <= 5; i++) {    archers.health += 0.1;    archers.trust += 0.08;    document.getElementById("rehab-bar").style.width = (archers.health * 100) + "%";    await sleep(400);  }  log("Rehabilitation successful. System stabilized.");  document.getElementById("run-btn").disabled = false;}