const nodeGrid = document.getElementById("nodeGrid");const firewallBar = document.getElementById("firewallBar");const rehabBar = document.getElementById("rehabBar");const logBox = document.getElementById("log");const runBtn = document.getElementById("runBtn");const NODE_COUNT = 10;function log(message) {  logBox.innerHTML += "• " + message + "<br>";  logBox.scrollTop = logBox.scrollHeight;}function createNodes() {  nodeGrid.innerHTML = "";  for (let i = 1; i <= NODE_COUNT; i++) {    const node = document.createElement("div");    node.className = "node";    node.textContent = `N${i}\nAlok Intelligence`;    nodeGrid.appendChild(node);  }}function resetSimulation() {  firewallBar.style.width = "0%";  rehabBar.style.width = "0%";  logBox.innerHTML = "";}function runSimulation() {  resetSimulation();  createNodes();  log("Initializing PolicyGrid Simulator...");  log("Nodes detected. Authority distributed.");    let firewall = 0;  let rehab = 0;  const interval = setInterval(() => {    if (firewall < 100) {      firewall += 20;      firewallBar.style.width = firewall + "%";      log("Eye-of-Shiv Firewall strengthening...");    } else if (rehab < 100) {      rehab += 20;      rehabBar.style.width = rehab + "%";      log("Rehabilitation cycle progressing...");    } else {      log("Authority stabilized. Network healed.");      clearInterval(interval);    }  }, 700);}runBtn.addEventListener("click", runSimulation);// Initial statecreateNodes();